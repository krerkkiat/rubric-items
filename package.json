{
  "name": "rubric-items",
  "version": "1.0.0",
  "description": "",
  "main": "Code.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/google-apps-script": "^1.0.27",
    "@types/google.script.client-side": "^0.1.1"
  },
  "scripts": {
    "build": "cd src-gas && tsc --project tsconfig.json --outDir ../dist",
    "build:client": "cd src-client && tsc --project tsconfig.json --outDir ../dist",
    "copy": "cp src-client/*.html dist/ && cp appsscript.json dist/ && cp .claspignore dist/ && cp .clasp.json dist/ && sed -i -e '/marker-replace-with-compiled-js/r ./dist/ClientCode.js' ./dist/JavaScript.html && sed -i 's/marker-replace-with-compiled-js//' ./dist/JavaScript.html"
  }
}

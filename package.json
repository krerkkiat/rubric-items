{
  "name": "rubric-items",
  "version": "1.0.0",
  "description": "",
  "main": "Code.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/google-apps-script": "^1.0.27",
    "@types/google.script.client-side": "^0.1.1"
  },
  "scripts": {
    "build": "cd src && tsc --project tsconfig.json --outDir ../dist",
    "build:client": "cd src-client && tsc --project tsconfig.json --outDir ../dist",
    "copy": "./scripts/copy.sh",
    "push": "cd dist/ && clasp push"
  }
}
